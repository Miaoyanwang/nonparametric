% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NonMar.R
\name{ASSIST}
\alias{ASSIST}
\title{Aggregation of structured sign series for trace regression (ASSIST)}
\usage{
ASSIST(X,y,X_new=NULL,r,sparse_r,sparse_c,H=10,lambda=0,rho.ini=0.1,min,max)
}
\arguments{
\item{X}{A list of predictor matrices.}

\item{y}{A vector of response variables.}

\item{X_new}{A list of new matrices at which predictions are to made. \code{X_new = NULL} is regarded as \code{X_new = X}.}

\item{r}{A rank of sign representable function to be fitted.}

\item{sparse_r}{The number of zero rows in coefficient matrix.}

\item{sparse_c}{The number of zero columns in coefficient matrix.}

\item{H}{Resoution parameter.}

\item{lambda}{Lagrangian multiplier.}

\item{rho.ini}{Initial step size.}

\item{min}{Minimum value of the response variables}

\item{max}{Maximum value of the response variables.}
}
\value{
The returned object is a list of components.

\code{B_est} - An array of which slices are the estimated coefficient matrix at each level.

\code{fitted} - The estimated responses at the predictor Xnew.

\code{sign_fitted} - A matrix of which rows are the sign of responses shifted by each level.
}
\description{
Implement ASSIST based on ADMM algorithm.
}
\examples{
######### Generate predictor matrices ###########################
X = list()
for(i in 1:10){
 X[[i]] = matrix(runif(4,-1,1),nrow = 2,ncol = 2)
}

######### Generate coefficient matrix ###########################
B = runif(2,-1,1)\%*\%t(runif(2,-1,1))

######### Generate response variables ###########################
y = NULL;signal = NULL
for(i in 1:10){
 signal = c(signal,sum(X[[i]]*B))
 y = c(y,sum(X[[i]]*B)+rnorm(1,sd = 0.1))
}


######### Run ASSIST ############################################
res =ASSIST(X,y,r = 1,sparse_r = 0,sparse_c = 0,min = min(y),max = max(y))
mean(abs(res$fitted-signal))


######### Generate new matrices at which predictions are to made #
X_new = list()
for(i in 1:10){
  X_new[[i]] = matrix(runif(4,-1,1),nrow = 2,ncol = 2)
}

######### Generate response variables from X_new ################
y_new = NULL
for(i in 1:10){
  y_new = c(y_new,sum(X_new[[i]]*B))
}

######### Run ASSIST #############################################
res =ASSIST(X,y,X_new,r = 1,sparse_r = 0,sparse_c = 0,min = min(y),max = max(y))
mean(abs(res$fitted-y_new))

}
\references{
Lee, C., Li, L., Zhang, H., and Wang, M. (2021). Nonparametric Trace Regression via Sign Series Representation. \emph{arXiv preprint arXiv:2105.01783}.
}
